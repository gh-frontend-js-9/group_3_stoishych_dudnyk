export interface Author {
    description: string;
    imgUrl?: null | string;
    _id: string;
    firstName: string;
    lastName: string;
    email: string;
    updatedAt: string;
    createdAt: string;
}
